//Write a program that can find the length the total length of a string, use a function.

function totalStringLength(str) {
    const nameLength = str.length;
    return nameLength;
}
const fullName = "Shimu Ziasmin";
console.log(totalStringLength(fullName));